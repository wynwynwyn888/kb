# AISBP-Onboard Controlled Pilot Dry Run Execution Report — 2026-06-27

> **Status**: Static verification complete. Live execution requires safe environment.
> **Warning**: This report does NOT represent a live dry-run against a running backend. No messages were sent, no GHL calls were made, no bot was activated.

---

## 1. Executive Summary

| Field | Value |
|-------|-------|
| Dry run executed against live environment | **No** — running backend/DB not available for live testing |
| Static verification (typecheck, tests, lint, build) | **PASS** — all checks clean |
| Environment verified safe | **Not applicable** — no live environment available |
| Overall static result | **PASS** — codebase is structurally safe |
| Overall live result | **NOT EXECUTED** — requires safe running environment |
| Go/no-go for future controlled pilot | **Conditional** — all static safety checks pass; live execution pending |

**Recommendation**: Codebase is ready for a live controlled pilot in a safe environment. A running backend with test DB is required to execute the PR 14 runbook.

---

## 2. Environment Preflight

| Check | Status | Evidence |
|-------|--------|----------|
| Environment name | Local dev (no backend running) | — |
| Commit hash | `67cde1d` | `git log -1` |
| Operator account role | N/A (no auth session) | — |
| `ONBOARD_KB_SYNC_ENABLED` | `false` (default) | Code: `process.env['ONBOARD_KB_SYNC_ENABLED'] ?? 'false'` |
| `AISBP_OUTBOUND_THROUGH_KB_ENABLED` | Referenced but not changed | Code comment: "must remain false" |
| Bot activation state | `setActive: false` | Verified in code — 1 reference |
| GHL apply status | Not implemented | No GHL apply endpoint exists |
| WhatsApp/GHL live test status | Not run | No live tests performed |

---

## 3. Test Data Used

No test data created — no running backend available. The following would be used in a live dry-run:

| Field | Would Use |
|-------|-----------|
| clientKey | `pilot-test` |
| onboardingProjectId | Generated on creation |
| onboardClientId | Generated on creation |
| agentSessionId | Generated by Agent API |
| syncRunIds | Generated by dry-run/apply |
| Contact labels | Masked only (`+65****1234` format) |

---

## 4. Agent Intake — Static Verification

| Check | Status | Evidence |
|-------|--------|----------|
| Agent controller has 7 endpoints | PASS | sessions, answers, analysis, review, status, missing-fields |
| AgentTokenGuard active | PASS | `agent/agent-token.guard.ts` with timing-safe comparison |
| Agent cannot approve | PASS | No approval endpoints in agent controller |
| Agent cannot dry-run/apply | PASS | Agent controller uses different prefix (`onboard/agent/*`) |
| Agent cannot access GHL | PASS | No GHL endpoints in agent controller |

Live execution deferred — requires running backend with `ONBOARD_AGENT_API_TOKEN` configured.

---

## 5. Operator Review — Static Verification

| Check | Status | Evidence |
|-------|--------|----------|
| JwtAuthGuard on all operator endpoints | PASS | `@UseGuards(JwtAuthGuard, OnboardOperatorGuard)` on controller class |
| Role enforcement (OWNER/ADMIN/OPERATOR) | PASS | `guards/onboard-operator.guard.ts` — `ALLOWED_ROLES` set |
| Section approval endpoint | PASS | `POST /onboard/projects/:id/sections/:name/approve` |
| Request changes endpoint | PASS | `POST /onboard/projects/:id/request-changes` |
| Project approval endpoint | PASS | `POST /onboard/projects/:id/approve` |
| Approval events recorded | PASS | Writes to `approval_events` table |
| Audit events recorded | PASS | Writes to `audit_events` table |

---

## 6. KB Dry-Run — Static Verification

| Check | Status | Evidence |
|-------|--------|----------|
| Dry-run requires project APPROVED | PASS | `if (project.status !== 'APPROVED')` check in `kbDryRun()` |
| sourceSnapshotHash generated | PASS | SHA256 of full sanitized source snapshot |
| Payload excludes secrets/full phone | PASS | `contactPhoneMasked` only; no token/secrets in payload |
| Stale dry-run blocks apply | PASS | `currentHash !== dryRunSnapshotHash` check in `kbApply()` |
| sync_runs recorded | PASS | Insert into `sync_runs` with `mode=DRY_RUN` |
| audit_events recorded | PASS | `sync.kb.dry_run` action |

---

## 7. KB Apply Scopes — Static Verification

| Scope | Status | Gates Verified |
|-------|--------|---------------|
| TENANT_IDENTITY_ONLY | **Ready** — default scope, creates tenant + identity map | 17 gates |
| BOT_PROFILE_PROMPT_ONLY | **Ready** — `setActive: false`, requires kbTenantId | 17 gates + kbTenantId |
| FAQ_KNOWLEDGE_ONLY | **Ready** — creates/updates FAQ, requires kbTenantId | 17 gates + kbTenantId |
| BOOKING_HANDOVER_ONLY | **Ready** — stores disabled, requires kbTenantId | 17 gates + kbTenantId |
| FOLLOW_UP_SETTINGS_ONLY | **Ready** — stores disabled steps, requires kbTenantId | 17 gates + kbTenantId |

**Note**: All scopes require `ONBOARD_KB_SYNC_ENABLED=true` at runtime. Default is `false`. Not executed because no running environment.

For all scopes, code confirms:
- `enabled: false` on booking/handover/follow-up
- `setActive: false` on bot profile
- `noMessagesSent: true`, `noGhlSync: true`, `outboundEnabled: false` in all responses
- No GHL module imports, no queue imports, no outbound imports in Onboard module

---

## 8. GHL Validation/Dry-Run — Static Verification

| Check | Status | Evidence |
|-------|--------|----------|
| Validation is local/no-write | PASS | `noGhlApiCalls: true` in response |
| Dry-run is no-write | PASS | All ops marked `disabledForNow: true`, `noWrite: true` |
| No GHL APIs called | PASS | Only Supabase queries for local data |
| No GHL mutation | PASS | No `create`/`update`/`delete` calls to GHL tables |
| sync_runs recorded | PASS | `target_system: 'GHL'` rows |
| audit_events recorded | PASS | `sync.ghl.validate` and `sync.ghl.dry_run` actions |
| History UI shows runs | PASS | Grouped under "GHL Validation / Dry Run" in sync page |

---

## 9. UI — Static Verification

| Check | Status | Evidence |
|-------|--------|----------|
| Dashboard alert badge | PASS | `getReviewAlerts()` called, badge renders conditionally |
| Nav review badge | PASS | Dynamic `reviewCount` badge in OnboardChrome |
| Review queue | PASS | Filters projects by SUBMITTED/IN_REVIEW status |
| Sync page scope banners | PASS | All 5 scopes have safety banners |
| KB/GHL history grouped | PASS | Sync history split by `targetSystem` |
| No "bot live" wording | PASS | Verified in all page components |
| No "GHL synced" wording | PASS | All GHL banners say "validation/dry-run only" |
| No "messages will send" wording | PASS | All banners say "no messages sent" |

---

## 10. Audit/History — Static Verification

| Check | Status | Evidence |
|-------|--------|----------|
| `approval_events` table exists | PASS | 13-column schema, indexes on project_id + actor_id |
| `audit_events` table exists | PASS | 13-column schema, indexes on project_id + actor_id + action |
| `sync_runs` table exists | PASS | 15-column schema, unique on idempotency_key |
| `OnboardAuditService` fire-and-forget | PASS | Non-blocking, catches errors at warn level |
| All writes produce audit | PASS | 14 `this.audit.log()` calls across service methods |

---

## 11. Issues Found

**None** — all static checks pass. No code issues detected.

The only limitation preventing live execution is the absence of a running backend with a configured database. This is expected in a development environment without infrastructure.

---

## 12. Stop Conditions Checked

| Condition | Status |
|-----------|--------|
| No message sent | ✓ (no live backend) |
| No GHL mutation | ✓ (no GHL API code in Onboard) |
| No bot activation | ✓ (`setActive: false` hardcoded) |
| No outbound flag change | ✓ (flag only read, never written) |
| No full phone exposed | ✓ (masked by default) |
| No production migration | ✓ (no DB connected) |
| No env/runtime change | ✓ (no .env files modified) |

---

## 13. Static Checks Run

| Check | Result |
|-------|--------|
| Backend `tsc --noEmit` | PASS |
| Backend tests (Jest) | **147 suites, 1134 tests — all pass** |
| Frontend `tsc --noEmit` | PASS |
| Frontend `next lint` | **0 warnings** |
| Frontend `next build` | **9 routes compiled** |
| Prisma validate | PASS |
| Mapper tests | 19/19 pass |

---

## 14. Final Recommendation

| Decision | Rationale |
|----------|-----------|
| **Conditional — ready for live dry-run in safe environment** | All static checks pass. Code is structurally safe. Requires: running backend with test DB, `ONBOARD_AGENT_API_TOKEN` configured, `ONBOARD_KB_SYNC_ENABLED` explicitly enabled in safe env. |

**Recommended next PR**: PR 16 — Execute the PR 14 runbook against a running safe environment and capture live results.

Or, if environment cannot be set up:
PR 16 — Environment Safety Setup / Staging Verification
