# Copy to infra/vps/.deploy.local.env (gitignored) for local push + VPS SSH deploy.
# Never commit .deploy.local.env.

# GitHub (demodropai/aisbp)
GITHUB_REPO=demodropai/aisbp
GITHUB_PAT=

# VPS SSH (Hostinger — srv1267113)
SSH_HOST=
SSH_USER=root
SSH_PASS=

# App on server
VPS_AISBP_PATH=/root/aisbp
